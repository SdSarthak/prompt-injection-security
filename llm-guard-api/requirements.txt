# Core runtime: the guard, the baseline classifier and the HTTP API.
# Nothing here needs a GPU or downloads a model at import time.

# Baseline classifier (TF-IDF + logistic regression) and data handling
scikit-learn>=1.3,<2
numpy>=1.24,<3
pandas>=2.0,<3
joblib>=1.3,<2

# HTTP service
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
pydantic>=2.5,<3

# Gemini client. 0.8+ is required for the gemini-2.0-* model names.
google-generativeai>=0.8.3,<1

# Configuration
python-dotenv>=1.0,<2
